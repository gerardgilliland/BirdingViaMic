<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/linen</item>
        <item name="colorPrimaryDark">@color/sienna</item>
        <item name="colorAccent">@color/teal</item>
        <item name="colorControlActivated">@color/teal</item>
        <item name="colorControlNormal">@color/sienna</item>
        <item name="colorControlHighlight">@color/linen</item>
        <item name="popupTheme">@style/AppTheme.ActionBar.Popup</item>
        <!--
         You can also set colorControlNormal, colorControlActivated
        <item name="android:navigationBarColor">@color/teal</item>
             colorControlHighlight & colorSwitchThumbNormal. -->
    </style>
    <!--
    <style name="AppTheme.Toolbar.Title" parent="AppTheme">
        <item name="android:textSize">@dimen/abc_text_size_title_material_toolbar</item>
        <item name="android:gravity">center</item>
        <item name="android:layout_gravity">center</item>
    </style>

        <style name="CodeFont" parent="AppTheme">
            <item name="android:textColor">@color/linen</item>
        </style>

        <item name="android:background">@drawable/spinner_background_state</item>
        <item name="android:dropDownSelector">@drawable/spinner_state</item>

        <item name="android:background">?android:selectableItemBackground</item>
        <item name="android:dropDownSelector">?android:selectableItemBackground</item>
        <item name="android:background">@color/black</item>
    <style name="AppThemeDialog" parent="Theme.AppCompat.DialogWhenLarge">

        <item name="android:dialogTheme">@style/AppTheme</item>
        <item name="android:alertDialogTheme">@style/AppTheme</item>
    <item name="android:dialogTheme">@style/MyDialogTheme</item>
    <item name="android:alertDialogTheme">@style/MyDialogTheme</item>
    <item name="android:alertDialogTheme">@style/AlertDialog</item>
    try the following in Manifest under application:
    android:alertDialogTheme="@style/AlertDialog"

    <style name="AlertDialog" parent="AppThemeDialog">
        <item name="colorPrimary">@color/linen</item>
        <item name="colorPrimaryDark">@color/sienna</item>
        <item name="colorAccent">@color/teal</item>

    </style>
    -->

    <style name="ButtonBar" parent="AppTheme">
    </style>

    <style name="ButtonBarButton" parent="ButtonBar" >
    </style>


    <style name="AppThemeDialog" parent="Theme.AppCompat.Dialog">
        <item name="android:background">@color/black</item>
        <item name="android:dropDownSelector">@color/white</item>
        <item name="android:windowIsFloating">true</item>
        <item name="colorPrimary">@color/linen</item>
        <item name="colorPrimaryDark">@color/sienna</item>
        <item name="colorAccent">@color/teal</item>
        <item name="colorControlActivated">@color/teal</item>
        <item name="colorControlNormal">@color/sienna</item>
        <item name="colorControlHighlight">@color/linen</item>
    </style>

    <!-- Overflow drop down style -->
    <style name="AppTheme.ActionBar.Popup" parent="Widget.AppCompat.Light.PopupMenu.Overflow">
        <item name="android:background">@color/linen</item>
        <item name="colorPrimary">@color/teal</item>
    </style>

</resources>